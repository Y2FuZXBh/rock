version: 2.1
workflows:
  testing:
    jobs:
      - runner-test
jobs:
  runner-test:
    machine: true
    resource_class: y2fuzxbh/tester
    steps:
      - run: powershell.exe -c "write-output 'USER = test'"
      - run: powershell.exe -c "write-output 'BRANCH = test'"
version: 2.1
workflows:
  testing:
    jobs:
      - runner-test
jobs:
  runner-test:
    machine: true
    resource_class: y2fuzxbh/tester
    steps:
      - checkout
      - run:
          name: tester
          command: >
            powershell.exe \\\test.ps1;
            powershell.exe .\\\scripts\\\runner\\\run.ps1;
